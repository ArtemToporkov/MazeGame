class Game {

    function void start() {

        var Level firstLevel;
        var Level secondLevel;

        let firstLevel = Level.new("Level 1: The IRIT-RTF Redemption");
        do firstLevel.run();

        let secondLevel = Level.new("Level 2: Unexpected Turns");
        do secondLevel.run();

        do Output.moveCursor(11, 22);
        do Output.printString("CONGRATULATIONS!!!");

        return;
    }
}