class Map {
    field Array walls;

    constructor Map new(Array wallsLocation) {
        let walls = wallsLocation;
        return this;
    }

    method void dispose() {
        do walls.dispose();
        do Memory.deAlloc(this);
        return;
    }
}